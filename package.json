{
  "name": "starboard-monorepo",
  "version": "0.0.1",
  "private": true,
  "files": [],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --sort",
    "lerna": "lerna"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
